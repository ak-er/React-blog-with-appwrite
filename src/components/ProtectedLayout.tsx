import React from "react";

interface ContainerProps {
  children: React.ReactNode;
}

export const ProtectedLayout: React.FC<ContainerProps> = ({ children }) => {
  // TODO: Implement protected layout
  return <>{children}</>;
};
